//========== IMPORT ==========//

// React
import React from "react";

//========== COMPONENT ==========//

const Jumbotron = () => (
  <div className="jumbotron text-center ">
    <h1> P i c t u r e  S p a n </h1>
    <p>
      Click on an image to earn points, but don't click on any more than once!
    </p>
  </div>
);

//========== EXPORT ==========//

export default Jumbotron;
